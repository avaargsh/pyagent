# 架构约束

## 工具链

- Go 版本：`1.23+`
- 格式化：`gofmt`
- 基础验证：`go test ./...`

## 依赖策略

- 优先标准库
- 仅在明显降低复杂度时引入轻依赖
- 禁止为了未来扩展预埋重抽象

## 工程规则

- `cmd/` 仅做命令组装
- 业务逻辑进入 `internal/`
- 跨层调用统一传递 `context.Context`
- 错误统一保留上下文并包装根因
- 诊断信息输出到 `stderr`

## 安全要求

- API Key 仅从环境变量读取
- 日志默认脱敏
- 禁止把 `AGENTS.md` 当作机器配置源
